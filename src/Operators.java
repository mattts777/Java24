class Operators {
    public static void main(String[] args) {
        String searchKeywords = "";
        searchKeywords = searchKeywords + "Lotus Esprit\n"; //One way to concatenate strings
        searchKeywords += "Lamborghini Countach\n"; //Another way to concatenate strings (less typing)
        
        System.out.print(searchKeywords);
        
    }
}